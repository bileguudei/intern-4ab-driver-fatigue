import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors } from '../theme';
import type { TabName } from '../types';
const tabs: { name: TabName; label: string; icon: string }[] = [{ name: 'home', label: 'Нүүр', icon: '⌂' }, { name: 'history', label: 'Түүх', icon: '◷' }, { name: 'settings', label: 'Тохиргоо', icon: '⚙' }];
export function BottomNav({ active, onChange }: { active: TabName; onChange: (tab: TabName) => void }) { return <View style={styles.wrapper}><View style={styles.nav}>{tabs.map((tab) => { const selected = tab.name === active; return <Pressable key={tab.name} onPress={() => onChange(tab.name)} style={[styles.tab, selected && styles.selected]}><Text style={[styles.icon, selected && styles.activeText]}>{tab.icon}</Text><Text style={[styles.label, selected && styles.activeText]}>{tab.label}</Text></Pressable>; })}</View></View>; }
const styles = StyleSheet.create({ wrapper: { position: 'absolute', left: 24, right: 24, bottom: 16 }, nav: { height: 66, borderRadius: 34, backgroundColor: colors.surfaceAlt, borderColor: colors.borderBright, borderWidth: 1, flexDirection: 'row', padding: 6 }, tab: { flex: 1, borderRadius: 26, justifyContent: 'center', alignItems: 'center' }, selected: { backgroundColor: '#3B82F626' }, icon: { color: colors.textMuted, fontSize: 22, lineHeight: 24 }, label: { color: colors.textMuted, fontSize: 10, marginTop: 2 }, activeText: { color: colors.primary, fontWeight: '700' } });
